import { withStyles } from "@material-ui/core";

const styles = (theme) => ({});
function Reviews(props) {
	const { classes, data } = props;
	return <div className="flex flex-row min-w-full"></div>;
}
export default withStyles(styles)(Reviews);
