import { withStyles } from "@material-ui/core";
const styles = (theme) => ({
	root: {},
});

function BottomAboutContent(props) {
	const { classes } = props;
	return <div className="flex flex-row"></div>;
}
export default withStyles(styles)(BottomAboutContent);
